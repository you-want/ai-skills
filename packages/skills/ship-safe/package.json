{
  "name": "@ai-skills/ship-safe",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "ai-skill": true,
  "dependencies": {
    "@ai-skills/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "pnpm run build && node --test test/**/*.test.mjs"
  }
}
